import React from 'react';
import PropTypes from 'prop-types';

const Header = props => {
  return (
    <div>
      I'm the Header
    </div>
  );
};

Header.propTypes = {

};

export default Header;
